# Contributing

欢迎通过 issue 记录误检、漏检和新的拍摄条件，并通过 pull request 改进代码。

提交改动前请：

1. 不提交原始篮球视频、个人信息、模型密钥或大型模型权重。
2. 为算法变化说明适用场景，并分别记录进球事件级 precision/recall。
3. 增加或更新能证明行为的测试。
4. 运行 `python3 -m unittest discover -s tests -v`。
5. 保持原视频只读，新增输出不得默认覆盖现有文件。

问题报告最好包含视频参数、机位是否固定、篮筐像素大小、光线情况、期望事件时间和实际结果。可以提供脱敏的小片段或合成复现素材，但不要公开未经授权的人物视频。
