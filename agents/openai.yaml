interface:
  display_name: "Basketball Basket Timestamps"
  short_description: "Find basket timestamps and export SRT markers"
  default_prompt: "Use $basketball-score-clipper to identify when the ball enters the hoop and output a timestamp table and SRT subtitle markers for Jianying without creating video clips."
